{
  "name": "flaq-ai-creator-browser-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "pnpm --dir web/flaq-saas install --frozen-lockfile",
    "dev": "node scripts/dev.mjs",
    "dev:site": "WATCHPACK_POLLING=true pnpm --dir web/flaq-saas exec next dev -p 3000",
    "dev:extension": "vite --host 127.0.0.1 --mode development",
    "build": "pnpm run build:dev",
    "build:dev": "tsc -b && vite build --mode development",
    "build:production": "tsc -b && vite build --mode production",
    "build:site": "pnpm --dir web/flaq-saas build",
    "typecheck": "tsc -b",
    "typecheck:site": "pnpm --dir web/flaq-saas ts-check",
    "test": "vitest run",
    "smoke:site": "node scripts/smoke-site.mjs",
    "sync:site": "git subtree pull --prefix web/flaq-saas flaq-saas-template main --squash"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "latest",
    "@types/chrome": "latest",
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "typescript": "latest",
    "vite": "latest",
    "vitest": "latest"
  },
  "packageManager": "pnpm@10.5.2"
}
