# Required for `pnpm build:production`.
# Optional local override. pnpm dev uses this for the SaaS server, iframe URL, and CSP.
FLAQ_SITE_PORT=3000

# Required only for a production extension build.
VITE_SIDEPANEL_SITE_URL=https://your-flaq-site.example
