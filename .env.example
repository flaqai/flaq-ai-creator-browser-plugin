# Optional: shared port for `pnpm dev` only. Packaged builds do not use localhost.
FLAQ_SITE_PORT=3000

# Optional: traditional remote iframe target used only by `pnpm build:remote`.
VITE_SIDEPANEL_SITE_URL=https://your-flaq-site.example
